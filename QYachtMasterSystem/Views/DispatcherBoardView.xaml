<UserControl x:Class="QYachtMaster.Views.DispatcherBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}">
    <UserControl.Resources>
        <Style x:Key="BayCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Direction="270" Color="#5C1D24" Opacity="0.12"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="{DynamicResource HeaderBackgroundBrush}" CornerRadius="12" Padding="16,12" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚓" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{DynamicResource HeaderForegroundBrush}"/>
                    <StackPanel>
                        <TextBlock Text="Q-Yacht Master Multi-Bay Visual Dispatcher Board" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource HeaderForegroundBrush}"/>
                        <TextBlock Text="لوحة التوزيع المرئية للصيانة والأرصفة - مزامنة لحظية عبر الشبكة المحلية LAN P2P" FontSize="12" Foreground="#E5D2D5"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="🔄 Refresh Grid / تحديث" Command="{Binding RefreshCommand}" 
                            Background="{DynamicResource PrimaryButtonBrush}" Foreground="White" FontWeight="SemiBold" Padding="14,8" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Kanban Columns -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Bays}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="265" Margin="0,0,14,0" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Column Header -->
                                <Border Grid.Row="0" Background="{DynamicResource HeaderBackgroundBrush}" CornerRadius="6" Padding="10,8" Margin="0,0,0,10">
                                    <DockPanel LastChildFill="False">
                                        <TextBlock Text="{Binding BayName}" FontWeight="Bold" Foreground="{DynamicResource HeaderForegroundBrush}" FontSize="14" DockPanel.Dock="Left"/>
                                        <TextBlock Text="{Binding Description}" FontSize="10" Foreground="#E5D2D5" Opacity="0.85" DockPanel.Dock="Right" VerticalAlignment="Center"/>
                                    </DockPanel>
                                </Border>

                                <!-- Job Cards in Column -->
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding BoatCards}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Style="{StaticResource BayCardStyle}">
                                                    <StackPanel>
                                                        <Grid Margin="0,0,0,6">
                                                            <TextBlock Text="{Binding JobCardNo}" FontWeight="Bold" Foreground="{DynamicResource PrimaryButtonBrush}" FontSize="14"/>
                                                            <Border HorizontalAlignment="Right" Background="#F8ECEE" CornerRadius="4" Padding="6,2">
                                                                <TextBlock Text="{Binding RegNo}" FontSize="11" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                                            </Border>
                                                        </Grid>

                                                        <TextBlock Text="{Binding CustomerName}" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,4"/>
                                                        <TextBlock Text="{Binding MakeModel}" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,6"/>

                                                        <Border Background="#FAF3F4" CornerRadius="4" Padding="6,4">
                                                            <Grid>
                                                                <TextBlock Text="{Binding Status}" FontSize="11" Foreground="#8B263E" FontWeight="SemiBold"/>
                                                                <TextBlock Text="{Binding JobDate}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Right"/>
                                                            </Grid>
                                                        </Border>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
