<Window x:Class="QYachtMaster.Views.PasscodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter Passcode / أدخل رمز الدخول" 
        Height="220" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Passcode Required / مطلوب رمز المرور" 
                   Style="{StaticResource TitleBlockStyle}" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Enter passcode to proceed / يرجى إدخال رمز المرور للمتابعة:" 
                   Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,10"/>

        <!-- Passcode Input Box -->
        <PasswordBox x:Name="CodeInput" Grid.Row="2" FontSize="16" Height="35" VerticalAlignment="Center" Padding="5"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel / إلغاء" Click="OnCancelClick" Margin="0,0,10,0" Padding="15,7" Background="{DynamicResource DangerBrush}"/>
            <Button Content="Unlock / دخول" Click="OnUnlockClick" Padding="20,7" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
